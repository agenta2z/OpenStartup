# HOT Incident Deep Dive — Direct Jira Data Pull (Attempt) & Findings

> **Methodology:** Attempted direct `get_jira_issue` calls + JQL search on hello.atlassian.net via Atlassian MCP. Documented honestly where I could and could not get data.

---

## A. Access reality check

| Source attempted | Result | Confidence impact |
|---|---|---|
| `get_jira_issue('HOT-300352')` (×9 in batch-1) | ❌ All returned "Issue does not exist or you do not have permission to see it" | Cannot validate individual HOT details |
| `get_jira_issue('HOT-300710' etc.)` (×11 in batch-2) | ❌ Same access denial | Same |
| `get_jira_issue('PIR-300811')` | ❌ Same access denial | The PARENT PIR is restricted |
| `get_jira_issue('GAPF-1743')` | ✅ **SUCCESS** | Follow-up tickets in `GAPF` project ARE accessible |
| `get_jira_issue('GAPF-1708')` | ✅ **SUCCESS** | Same |
| `get_jira_issue('FD-188275')` | ✅ **SUCCESS** | `FD` (Feature Dial) project also accessible |
| JQL `project = HOT AND ...` | ❌ Returns 0 issues | HOT project not indexed for my account |
| JQL `key in (HOT-300438, HOT-300449, ...)` | ❌ Returns 0 issues | Confirmed: NOT a name issue |
| TWG NL2Cypher for HOT issues | ❌ Returns empty data array | Either auth-blocked or not indexed |
| TWG NL2Cypher for `conversational-ai-platform` PR data | ❌ **Returns empty data array** | **Repo NOT indexed in TWG** — confirmed via 2 separate queries |

**Implication:** All HOT incident detail I cite in `02_OPERATIONAL_SIGNALS.md` comes from the **synthesized RCA Confluence page (`gai/6980681738`)**, not from primary Jira data. The RCA itself was generated by Rovo Dev for @Hajin Kim on 2026-05-06 — it is a secondary source aggregating 14 weekly oncall logs.

**Confidence label:** All HOT-NNNNNN claims in this report should be marked **"per RCA, not independently verified against Jira"**.

---

## B. The 3 tickets I COULD access — full ground truth

These confirm the RCA's claims indirectly because they are the **explicit follow-up action items** for the convo-ai PIR-300811 cluster (HOT-301423, GAPF-1708 et al).

### B.1 GAPF-1743 — Restrictive Rate Limiting for Convo-AI

| Field | Value |
|---|---|
| Type | Task |
| Status | Done ✅ |
| Priority | Minor |
| Assignee | **Ryan Fallah** |
| Created | 2026-05-01 |
| Updated | 2026-05-18 |
| Parent | **PIR-300811** |

**Root cause (verbatim from ticket):** *"Traffic spike (~10x) from Hello instance to unpaginated endpoints (`GET /api/agents/v1` and `GET /api/rovo/v1/agents`). Hello's 16k agents overwhelmed convo-ai service."*

**Resolution:** *"Lower rate limits on problematic endpoints from 40k req/min catch-all rule. Consider per-tenant limits."*

**This confirms:**
- A SEV1-class incident occurred around 2026-04-16 (HOT-301423)
- Hello tenant has **16,000 agents** in production (huge cardinality)
- The pre-fix catch-all rate limit was **40,000 req/min** for `/agents/v1` & `/api/rovo/v1/agents`
- Per-tenant rate-limit was identified but not yet implemented

### B.2 GAPF-1708 — Update EC2 scaling rules

| Field | Value |
|---|---|
| Type | Task |
| Status | Done ✅ |
| Priority | Minor |
| Assignee | **Michal Huzevka** |
| Created | 2026-04-20 |
| Updated | 2026-05-18 |
| Parent | **PIR-300811** |

**Root cause:** *"Thread starvation led to manual over-provisioning (48+ nodes) during HOT-301423."*

**Resolution:** *"Implement automatic scaling based on thread starvation metrics and rebalance cost vs. over-provisioning."*

**This confirms:**
- During HOT-301423, convo-ai was manually scaled to **48+ nodes** (vs normal 10-15 per `convo-ai.ad.yml`)
- Auto-scale on thread-starvation metric was identified as the fix — matches my OPP-05 (staging thread-saturation alarm) recommendation

### B.3 FD-188275 — rovo_agents_only_fetch_one_big_page (Switcheroo FF)

| Field | Value |
|---|---|
| Type | New Feature |
| Status | Planning ⏳ |
| Priority | Minor |
| Assignee | **Aiden Haak** |
| Created | 2026-04-16 |
| Updated | 2026-04-17 |
| Purpose | Internal flag (hello-only) to mitigate HOT-301423 by limiting `/agents/v1` and `/api/rovo/v1/agents` to first **1000 agents** instead of all 16,000 |

**This confirms:**
- HOT-301423 occurred on **2026-04-16** (created date of mitigation flag)
- Flag is **hello-only** — Hello tenant treated as a special-case "noisy neighbor"
- Page-size cap of **1000 agents** chosen as the temporary remediation

---

## C. Derived corrections to my prior report

| Claim in earlier 02_OPERATIONAL_SIGNALS.md | Correction |
|---|---|
| "PIR-29582 (SignalFX metrics outage)" was the master PIR | **WRONG** — that was a *different, smaller* observability PIR. The master Convo-AI capacity incident is **PIR-300811** (Apr 16 cluster) |
| HOT-301423 was about "Tomcat exhaustion (generic)" | **REFINEMENT** — it was specifically triggered by Hello tenant's 16k agents being fetched unpaginated, causing 10× traffic spike from a single tenant |
| "Tomcat threads / node = 200" | ✅ Confirmed |
| "Reactor Netty event loops = 16" | ✅ Confirmed via HOT-300449 quote in RCA |
| "EC2 autoscale min/max" | **REFINEMENT** — during HOT-301423 the team manually scaled to **48+ nodes**, far above the configured max in `convo-ai.ad.yml`; auto-scaling on thread-starvation is the proposed permanent fix (GAPF-1708) |
| "Heimdall raised to 100K per asap_issuer" | (Not verified against Jira; remains a RCA-only citation) |
| "80M TPM cap" | (Not verified against Jira; remains a RCA-only citation) |

---

## D. New facts discovered (not in original report)

1. **PIR-300811 is the parent PIR cluster** — spawned at least GAPF-1743, GAPF-1708, FD-188275 (and likely more). Look for sibling tickets via "Has parent PIR-300811" JQL when account access permits.
2. **Hello has 16,000 Rovo agents in production** — single largest tenant by agent count. This is a **structural noisy-neighbor risk** for convo-ai and would benefit from explicit tenant-fairness work.
3. **Catch-all rate-limit was 40k req/min** before GAPF-1743 — now lowered. Future incidents likely from tenants below the new threshold but still abusive.
4. **Manual over-provisioning to 48+ nodes** was the playbook during HOT-301423 — this proves **OPP-05's scaling-first runbook proposal is well-founded** (it was the actual resolution).
5. **`rovo_agents_only_fetch_one_big_page` Switcheroo FF** exists — first 1000 agents only. Useful test surface for any future per-tenant fairness work.
6. **GAP/Hajin Kim are the natural owners** of the PIR-300811 cluster — RCA was generated for Hajin, and follow-up tickets land in `GAPF` project.

---

## E. Recommended next steps (data-collection)

1. **Request access** to the HOT and PIR projects on hello.atlassian.net (via @Hajin Kim or via the GAP team's Confluence space `gai`). This would upgrade every HOT-NNNNNN claim in this report from "secondary/RCA-only" to "Jira-verified".
2. **Set up TWG indexing** of `conversational-ai-platform` repo — confirmed via NL2Cypher that it's NOT currently indexed. Without TWG, OPP-06 (auto-revert candidate detector) cannot use TWG as its correlation backbone and would need to use the Bitbucket REST API directly (which works fine, just slower).
3. **Pull all siblings of PIR-300811** via JQL `"Parent" = PIR-300811` once access is granted — this is the cluster of follow-up action items I'd want to make sure are tracked against my opportunity list.
4. **Inspect the SignalFx dashboards** referenced in runbooks (e.g., `https://atlassian.signalfx.com/#/dashboard/G022QVxA4AA?...`) for live MTS history — would close the "0 live metric values" gap mentioned in the prior confidence audit.

---

## F. Honest caveat

> The bulk of my HOT analysis remains **derived from the RCA Confluence page (`gai/6980681738`)** plus the 3 GAPF/FD tickets I COULD access. Treat HOT-NNNNNN counts and one-line summaries as **the RCA author's classification**, not as primary-source data. The directional signal (categories, recurrence, root-cause classes) is highly likely correct because the RCA author is the convo-ai oncall lead and was synthesizing the team's own weekly logs — but specific dates, ticket states, and customer-impact numbers should be re-validated when Jira access permits.
