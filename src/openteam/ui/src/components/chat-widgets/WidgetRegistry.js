/**
 * WidgetRegistry — re-exports from @agent-foundation/shared-ui.
 *
 * The canonical extensible registry now lives in the shared library.
 * OpenStartup domain widgets are registered at app bootstrap in App.js.
 */

export { getWidget, registerWidget } from '@agent-foundation/shared-ui';
