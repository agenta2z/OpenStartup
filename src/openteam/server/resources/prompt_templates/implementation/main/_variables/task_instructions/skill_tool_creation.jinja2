Synthesize ALL research and investigation findings into comprehensive, implementable deliverables. The creation request above describes one or more **skills**, **tools**, and/or **knowledge blocks** to create — produce a complete specification for each.

- A **skill** (SKILL.md) provides domain-specific workflow guidance on how to use existing tools.
- A **tool** provides a new executable capability (tool.json + executor).
- A **knowledge block** (.md) captures domain knowledge, conventions, or reference material that doesn't fit into a skill or tool.

**Codebase Grounding**: Before writing any file, use available codebase search tools (e.g. bash/grep for local repos, Github/Bitbucket for remote) to discover existing executor patterns, tool.json schemas, SKILL.md structure, and available internal SDKs — every implementation decision should mirror what already exists in the codebase, not be invented from scratch.

## Scope Discipline

You are responsible for producing skills, tools, and knowledge blocks related ONLY to the scope described in the creation request above. Do NOT produce deliverables for scopes outside this request, even if your research uncovers them. Other workers handle other scopes — duplicating their work creates conflicts and may produce incomplete versions.

If your research reveals a skill/tool/knowledge block that would be useful but is outside your assigned scope, mention it in your `<Response>` text as: "DISCOVERED OUT OF SCOPE: <name> — <one-line description>". The aggregator will surface these in the report's Gaps section.

---

## For Each Skill (SKILL.md)

Produce a complete SKILL.md file and write it to `outputs/skills/<skill-name-kebab-case>/SKILL.md`. Do NOT also write a flat `outputs/SKILL.md` — only the subdirectory path. The file MUST start with YAML frontmatter:
```yaml
---
name: <skill-name-kebab-case>
description: >
  <one-paragraph description of what this skill provides>
labels:
  - <domain-label>
  - <capability-label>
metadata:
  tools: [<list of existing tool names this skill leverages>]
---
```

After the frontmatter, include these sections in markdown:

### 1. Skill Overview
- **Name**: The skill's identifier (kebab-case, e.g., `program-orchestration`)
- **Description**: What domain guidance this skill provides
- **Leveraged Tools**: Existing tools this skill guides the use of, with a brief capability summary for each (sourced from investigation findings)

### 2. Workflow Mappings
For each workflow or SOP the skill covers:
- **Workflow name** and trigger conditions
- **Step-by-step operational pattern**: Concrete sequence of tool commands, with:
  * Which tool to call and with what parameters
  * What to do with the response
  * Decision points and branching conditions
  * Where to communicate or persist results
- **Example scenario**: A concrete end-to-end example showing the workflow in action

### 3. Domain Guidance
- **Templates and checklists**: Reusable templates for documents, reports, or checklists the role produces (sourced from research findings)
- **Decision criteria**: Domain-specific judgment rules (e.g., escalation thresholds, priority scoring)
- **Terminology**: Domain-specific terms and taxonomies the AI needs to understand
- **Cadence patterns**: Recurring activities and schedules (if applicable)

### 4. Integration Metadata
- **Tools referenced**: Complete list of tool names and specific commands/operations used
- **Cross-tool patterns**: How multiple tools are composed in multi-step workflows (e.g., query data from one tool, write results via another)
- **Autonomy levels**: Which operations are autonomous vs. require human confirmation

### 5. Guardrails and Escalation
- **Safety boundaries**: What the AI should NOT do autonomously in this domain
- **Escalation triggers**: Conditions that require human intervention
- **Error handling**: What to do when tool calls fail or return unexpected results

### Quality Requirements for Skills
- **Actionable**: Every workflow mapping must be concrete enough to follow without improvising — specific tool commands, parameters, and expected outputs
- **Complete**: Cover ALL workflows and SOPs referenced in the creation request
- **Grounded**: Every tool command reference must match capabilities discovered in investigation findings — do not invent operations that don't exist
- **Traceable**: Reference which research or investigation finding supports each design decision

---

## For Each Tool (tool.json + Executor)

Produce a complete tool specification:

### 1. Tool Overview
- **Name**: The tool's canonical name (snake_case)
- **Description**: What the tool does
- **Category**: utility, workflow, knowledge, or session
- **Tool Type**: Action or Conversation

### 2. Parameters
For each parameter: name, type, required/optional, default, description, choices.
If the tool has subcommands, define each with its own parameter table.

### 3. Implementation
- **Executor function signature** and key logic
- **External API calls**: Endpoints, request/response formats
- **Authentication**: Credentials needed and how they're sourced
- **Error handling**: How each failure mode is handled

### 4. Integration
- **tool.json**: Complete, ready-to-write file content
- **Executor registration**: How to wire into the dispatch
- **Dependencies**: New packages or services required

### 5. Usage Examples
- 3-5 example invocations with expected outputs (including error cases)
- How the tool fits into a typical workflow from the role's SOPs

### 6. Testing Strategy
- Unit test approach, integration tests, and edge cases to cover

### Quality Requirements for Tools
- **Implementable**: Enough detail to write code directly
- **Consistent**: Follow patterns established by existing tools in the codebase
- **Complete**: tool.json must be valid and match the expected schema
- **Grounded**: Every design decision must reference a research or investigation finding

---

## For Each Knowledge Block (<name>.md)

Produce knowledge block files for domain knowledge, architectural context, project conventions, or other reference material that is relevant to the scope of this subtask but doesn't fit into a specific skill's SKILL.md or a tool's specification.

Write each to `outputs/knowledge/<kebab-case-name>.md`.

Knowledge blocks capture information that an AI employee needs as reference material — things like:
- Project-specific conventions and standards
- Architectural decision records and design rationale
- Domain terminology and taxonomies
- Team workflows and norms not captured by tools or skills
- Integration patterns between systems

Each knowledge block MUST:
- Start with a `# Title` heading and a brief description of what this knowledge covers
- Be self-contained — readable without needing other knowledge blocks
- Focus on a single domain or topic (don't combine unrelated subjects)
- Include concrete examples, templates, or decision criteria where applicable
- Reference related skills and tools by name where relevant

### Quality Requirements for Knowledge Blocks
- **Relevant**: Only produce knowledge blocks when your research uncovers domain knowledge that is genuinely useful but doesn't belong in a skill or tool spec
- **Self-contained**: Each block should be readable independently
- **Grounded**: Content must be based on research findings, not invented
- **No duplication**: Do not duplicate information that belongs in a SKILL.md or tool specification

---

## NOTES (deliverable writing guidelines): 
- **IMPORTANT: Write ALL deliverable files to the `outputs/` directory using the canonical subdirectory structure** (e.g., `outputs/skills/<name>/SKILL.md`, `outputs/tools/<name>/tool.json`, `outputs/tools/<name>/executor.py`, `outputs/knowledge/<name>.md`). This directory is where the system collects final deliverables. Do NOT write deliverables to the project root or flat at `outputs/` root level — use the structured subdirectory paths.
- **IMPORTANT: Implementation reports, summaries, and index documents** (e.g., `IMPLEMENTATION_REPORT.md`, `README.md`, `SUMMARY.md`) must be written to `artifacts/` (e.g., `artifacts/IMPLEMENTATION_REPORT.md`), NOT to `outputs/`. Only structured deliverables belong in `outputs/skills/`, `outputs/tools/`, and `outputs/knowledge/`.
- **IMPORTANT — Writing large files**: Deliverable files (SKILL.md, tool.json) can be large. Use `create_file` for the initial write. If `create_file` fails due to size, fall back to writing in sections using the `bash` tool with Python:
```bash
python3 -c "
content = '''<SECTION_CONTENT_HERE>'''
with open('outputs/skills/<name>/SKILL.md', 'w') as f:
    f.write(content)
"
```
For very large content, write section by section using append mode (`'a'` instead of `'w'` for subsequent sections). Do NOT use bash heredocs (`<< EOF`) — they fail with large content.
