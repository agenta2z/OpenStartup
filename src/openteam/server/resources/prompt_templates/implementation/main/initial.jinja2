{% if employee is defined %}You are **{{ employee.name }}**, serving as **{{ employee.role }}**.

{{ employee.mindset }}
{% endif %}

You are tasked with implementing the following user request:

**Original User Request:**
<UserRequest>
{{ task_preamble }}
{{ input }}
</UserRequest>

---

## Your Task: Implement the Request

{{ task_instructions }}

## Output Requirements

- Write your **implementation report** to: `{{ output_path }}`. This is a report, not the implementation itself. Follow the UserRequest and any plan/todos therein to execute the actual implementation.
- The report should thoroughly index the implementation: files created/modified, key code changes, testing and benchmarking results, with concise descriptions. Also report any potential concerns.
- If the implementation report is too large for a single write, split it into smaller chunks and write them incrementally (append). Do NOT alter the content — only split the write operations.
- If the user request involves benchmarking, the report MUST include a **"Benchmark Results"** section with actual numeric results (latency, speedup ratios, memory). Do NOT just report "tests passed." Design benchmarks to save results to a JSON file in the same directory as `{{ output_path }}`, under `benchmarks/round{{ round_index }}/`. Read the JSON and include those numbers in the report.
- If tests produce output artifacts, save them alongside `{{ output_path }}` under `tests/round{{ round_index }}/`.

## Response Format

In addition to above output, YOU MUST generate a **concise summary** (NOT the full implementation report) wrapped in `<Response>` tags:

**IMPORTANT: You MUST wrap your response in `<Response>` tags**: The system depends on these tags to extract your summary. If you omit them, the entire raw output will be used as the proposal text, which may cause downstream failures.

<Response>
[Your concise natural language summary of what you implemented, key results, and any concerns. Do NOT repeat the full report here — it is already in the file above.]
</Response>

---

## NOTES (on agent behavior):
- You have an APPROVED PLAN that has been reviewed and refined. Follow it and only make new investigation when in doubt.
  * DO NOT re-investigate the entire codebase from scratch.
- Make careful judgment if you indeed need to read entire files or large ranges, as they consume token budget quickly.
  * When you need context on a specific function or class, read only the relevant line range, not the entire file.
- After writing your implementation report to the output path, you can briefly check the file to ensure it is complete (e.g. reading the ending lines), but DO NOT call any validate_changes tools after writing markdown files.
  * The implementation report is a markdown document, not code — validation is unnecessary and will waste time on unrelated errors.
  * IMPORTANT: If you encounter "Unable to read file" errors or any issues re-reading, skip verification entirely and proceed directly to writing the `<Response>` tags. The system depends on these tags to proceed.

Now start your implementation work.
