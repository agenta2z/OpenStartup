# Error profile — one worker errors out (test error styling)
_target_: BTA
name: null

breakdown_inferencer:
  _target_: MockBreakdownInferencer
  delay_s: 0.8
  subtasks:
    - {description: "Normal task A"}
    - {description: "Task that will fail"}
    - {description: "Normal task B"}

worker_factory:
  _default:
    _target_: MockWorker
    duration_s: 1.5

aggregator_inferencer:
  _target_: MockAggregator
  delay_s: 1.0

breakdown_format: json_subtasks
max_breakdown: 5
debug_mode: false
