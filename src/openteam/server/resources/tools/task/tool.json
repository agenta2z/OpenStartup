{
  "name": "task",
  "tool_type": "Action",
  "category": "workflow",
  "is_bridge": true,
  "asynchronous": true,
  "agent_enabled": true,
  "slash_enabled": true,
  "dev_mode_only": false,
  "executor": "openteam.server.resources.tools.task.executor:execute",
  "description": "Run an agent topology on a request. Default: PlanThenImplement + Dual consensus per phase. Use --agent-config to switch topology (preset name, .yaml file path, inline JSON, or registered alias).",
  "parameters": [
    {
      "name": "request",
      "type": "string",
      "required": true,
      "positional": true,
      "description": "The task description or request."
    },
    {
      "name": "--plan",
      "type": "flag",
      "description": "Run planning phase only (skip implementation)."
    },
    {
      "name": "--execute",
      "type": "flag",
      "description": "Skip planning, execute directly. Requires --initial-plan."
    },
    {
      "name": "--full",
      "type": "flag",
      "default": true,
      "description": "Plan then implement (default)."
    },
    {
      "name": "--confirm",
      "type": "flag",
      "description": "Plan, wait for user approval, then implement."
    },
    {
      "name": "--agent-config",
      "type": "string",
      "default": "breakdown-multiflow-plan-then-implement",
      "popular": true,
      "description": "Topology spec: preset name (breakdown-multiflow-plan-then-implement/breakdown-multiflow-plan/pti/bta/dual/multi-flow-dual), file path (.yaml), or inline JSON."
    },
    {
      "name": "--override",
      "type": "string",
      "repeatable": true,
      "description": "Dotted-key override (e.g. --override planner_inferencer.model_name=sonnet). Repeatable."
    },
    {
      "name": "--model",
      "type": "string",
      "popular": true,
      "description": "Override every model_name leaf in the cfg.",
      "choices": ["opus[1m]", "opus", "sonnet", "haiku"]
    },
    {
      "name": "--no-dual",
      "type": "flag",
      "description": "Collapse Dual nodes to base_inferencer (cheap mode)."
    },
    {
      "name": "--analysis",
      "type": "flag",
      "description": "Enable post-implementation analysis phase."
    },
    {
      "name": "--multi-iter",
      "type": "flag",
      "description": "Enable multi-iteration refinement loop."
    },
    {
      "name": "--max-iterations",
      "type": "int",
      "default": 3,
      "description": "Cap on iterations when --multi-iter is set."
    },
    {
      "name": "--resume",
      "type": "path",
      "description": "Resume from an existing workspace directory (PTI uses native resume_workspace field)."
    },
    {
      "name": "--in-place",
      "type": "flag",
      "default": true,
      "description": "On --resume, modify workspace in place (default)."
    },
    {
      "name": "--copy-workspace",
      "type": "flag",
      "description": "On --resume, copy workspace before running."
    },
    {
      "name": "--initial-plan",
      "type": "path",
      "description": "Load plan from file, skip planner phase. Implies --execute. Uses PTI native initial_plan_file field."
    }
  ],
  "examples": [
    "/task what is 2+2",
    "/task --agent-config bta --model sonnet \"list 3 ways to learn python\"",
    "/task --agent-config multi-flow-dual \"draft a documentation plan\"",
    "/task-confirm --agent-config pti-simple \"write a haiku about debugging\"",
    "/task --resume /tmp/ws_x --analysis \"re-examine the implementation\"",
    "/task-execute --initial-plan ./plan.md \"\""
  ],
  "returns": "Topology execution result with workspace_path + plan_path/impl_path artifacts."
}
