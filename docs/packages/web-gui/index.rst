web-gui
=======

.. note::

   **Package type:** UI  |  **Python module:** ``web_gui``

Browser-based control plane that surfaces inference workflows, scout results, and code agents through a unified UI.

This documentation set is generated by the OpenStartup ``docs/`` Sphinx build.
It uses :mod:`sphinx.ext.autodoc` with locally-declared ``autodoc_mock_imports``
so that heavy third-party dependencies (Atlassian SDKs, MCP runtime, web
frameworks) do not need to be installed in the docs build environment.

.. toctree::
   :maxdepth: 2
   :caption: Contents

   overview
   architecture
   api
   usage
   frontend
   integration
   development

Quick links
----------------------------------------

* **Source:** ``packages/web-gui/``
* **Entrypoints:**

  * ``web-gui``: CLI launcher (uvicorn wrapper)
  * ``web_gui.app:create_app``: FastAPI application factory

* **Integrates with:** FastAPI, Vite + React frontend, Tailwind CSS, uvicorn / gunicorn
